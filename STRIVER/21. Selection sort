
def selection_sort(nums):
    n = len(nums)
    
    # Traverse through all elements of the list
    for i in range(n):
        # Assume the minimum element is at position i
        min_index = i
        
        # Find the index of the smallest element in the remaining unsorted part
        for j in range(i + 1, n):
            if nums[j] < nums[min_index]:
                min_index = j
        
        # Swap the found minimum element with the first unsorted element
        nums[i], nums[min_index] = nums[min_index], nums[i]
    
    return nums

# Example
nums = [7, 4, 1, 5, 3]
print(selection_sort(nums))  # Output: [1, 3, 4, 5, 7]



nums[i], nums[min_index] = nums[min_index], nums[i]
This moves the smallest element into the correct position in the sorted portion.

